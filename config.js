// Colors now live in Colors.qml (pywal-driven singleton)

const bar = {
    fontFamily: "Mononoki Nerd Font",
    fontSize: 22,
    height: 30
}


const notifications = {
    timeout: 5000
}

const timer = {
	interval: 1000
}